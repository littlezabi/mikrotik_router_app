import React from "react";

function ExampleComponent() {
    return (
        <div>
            <h1>Hello from React!</h1>
            <p>This is a React component embedded in a Laravel application.</p>
        </div>
    );
}

export default ExampleComponent;
